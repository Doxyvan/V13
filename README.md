## О проекте

Данный проект реализует гибридный автоэнкодер, сочетающий реконструкцию изображений и контрастивное обучение. Основная цель - исследование компромисса между качеством реконструкции и способностью к кластеризации в латентном пространстве.

**Ключевые особенности:**
- Гибридная функция потерь: `L = L_recon + λ · L_contrast`
- Контрастивное обучение с использованием InfoNCE loss
- Эксперименты с различными значениями λ и температуры
- Визуализация латентных пространств и реконструкций

## 🎯 Цели исследования

1. Реализовать автоэнкодер с контрастивным обучением на MNIST
2. Исследовать влияние параметра λ на баланс между реконструкцией и кластеризацией
3. Проанализировать эффект температурного параметра в InfoNCE loss
4. Визуализировать структуру латентного пространства при различных настройках

## 🏗️ Архитектура

### Модель автоэнкодера:
- **Энкодер**: Сверточная сеть, сжимающая изображение 28×28 до латентного вектора размерности 32
- **Декодер**: Сверточная сеть, восстанавливающая изображение из латентного вектора

### Функция потерь:
```python
L_total = L_recon + λ · L_contrast
L_recon = MSE(original, reconstructed)
L_contrast = InfoNCE(z_original, z_augmented, temperature=τ)
